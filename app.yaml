runtime: custom
env: flex

entrypoint: gunicorn -b :8080 app:server

automatic_scaling:
  target_cpu_utilization: 0.75
  min_num_instances: 1
  max_num_instances: 3

resources:
  cpu: 2
  memory_gb: 8
  disk_size_gb: 10

network:
  session_affinity: true
